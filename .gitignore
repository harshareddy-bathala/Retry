# Dependencies
node_modules/
.pnpm-store/

# Env & secrets — .env.example is the only committed env file
.env
.env.*
!.env.example

# Builds
dist/
build/
*.tsbuildinfo
apps/web/dist/
apps/api/dist/

# Test & coverage
coverage/
playwright-report/
test-results/

# Licensed art (LimeZu "Modern Interiors", limezu.itch.io)
# This private repository tracks both the original source pack and generated runtime
# assets. If it is ever made public, remove both from history first.

# Local data
uploads/
*.local

# Logs
*.log
logs/

# Editor / OS
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/settings.local.json
# Scratch git worktrees — separate checkouts, never content of this one
.claude/worktrees/


# Playwright drive output — traces and screenshots of a local run
apps/e2e/test-results/
apps/e2e/playwright-report/
