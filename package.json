{
  "name": "foundry",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter @foundry/api --filter @foundry/room-server --filter @foundry/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "format": "prettier --write \"**/*.{ts,tsx,md,json,yml,css}\"",
    "db:generate": "pnpm --filter @foundry/db generate",
    "db:migrate": "pnpm --filter @foundry/db migrate",
    "db:seed": "pnpm --filter @foundry/db seed"
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  }
}
